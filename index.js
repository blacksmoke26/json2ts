/**
 * JSON to TypeScript Converter
 *
 * This module provides functionality to convert JSON objects into TypeScript interfaces.
 * It supports both nested and flattened interface generation.
 */
module.exports = require('./lib/index');
